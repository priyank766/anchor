{
  "name": "anchor",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "description": "Cross-agent memory layer. Local-first MCP server + open Skill so any agent (Claude Code, Codex, Cursor, Cline, Antigravity, ...) shares context.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
